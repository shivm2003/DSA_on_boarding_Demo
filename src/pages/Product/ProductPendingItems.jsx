import React from 'react';
import './ProductDashboard.css';

const ProductPendingItems = () => (
  <div className="product-dashboard-page">
    <h1 className="page-title">Pending Items</h1>
    <p className="text-muted">Review items that need action in the product workflow.</p>
  </div>
);

export default ProductPendingItems;
